{
  "rules": {
    "users": {
      ".read": true,
      ".write": "auth != null",
      ".indexOn": ["email", "deviceId", "license"],
      "$uid": {
        ".read": "auth != null && auth.uid == $uid",
        ".write": "auth != null && auth.uid == $uid"
      }
    }
  }
}
